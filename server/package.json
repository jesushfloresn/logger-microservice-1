{
  "name": "server",
  "version": "1.0.0",
  "description": "Logger microservice",
  "main": "server.js",
  "scripts": {
    "dev": "NODE_ENV=dev nodemon server.js",
    "debug": "NODE_ENV=dev nodemon --inspect server.js",
    "start": "NODE_ENV=prod node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joel Davila Lopez",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "pg": "^8.6.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
